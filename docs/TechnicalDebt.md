# Technical Debt Register

Living document. Every item here is either fixed (moved to "Resolved") or
still open. Nothing here should exist only in conversation or memory --
if it's known, it's written down.

## Open

| ID | Category | Description | Impact | Remediation | Priority |
|---|---|---|---|---|---|
| TD-001 | Dependency risk | `orion_kernel` currently has exactly one consumer (CareerOS backend). Its function signatures were designed correctly for that one case but are unvalidated against a second product's needs. | Low today; the first real second product may require breaking changes to kernel interfaces. | Do not over-generalize speculatively (see `orion-governance/architecture/ArchitecturePrinciples.md` #7); revisit kernel API surface when the second product is actually authorized. | Low |
| TD-002 | Security | No role-based authorization anywhere on the platform. Auth framework establishes identity only. | Blocks any multi-user or multi-tenant phase; also blocks Audit (kernel responsibility) which depends on knowing what a user was allowed to do. | Implement Authorization as a Platform Kernel capability (see `docs/platform/PLATFORM_KERNEL.md`) when a sprint authorizes multi-user work. | Medium (not urgent for single-user Sprint 1/1-Closure scope) |
| TD-003 | Security | No rate limiting or account lockout on `/auth/login` or `/auth/register`. | Acceptable for local single-user use; a real risk if ever exposed beyond localhost. | Add rate limiting (e.g. `slowapi` or a reverse-proxy-level limit) before any externally-reachable deployment. | Medium |
| TD-004 | Security | No dependency/CVE scanning configured (e.g. `pip-audit`, `npm audit` is available ad hoc but not gated in CI because no CI exists yet -- see TD-006). | Unknown vulnerable dependencies could go unnoticed. | Add `pip-audit` and `npm audit --audit-level=high` as CI steps once CI exists. | Medium |
| TD-005 | Configuration | Default `SECRET_KEY` in `.env.example` is an intentionally insecure placeholder. | Would be a real vulnerability if deployed unchanged. | Documented in README and `.env.example`; enforce via a startup check that refuses to boot with the placeholder value when `ENVIRONMENT=production`. | Medium |
| TD-006 | Process | No CI pipeline exists. All verification in this session was run manually (pytest, tsc, embedded-Postgres smoke test). | Verification is not automatically re-run on every change; regressions could be reintroduced silently. | Add a minimal CI workflow (pytest + tsc -b) -- recommended as the first Sprint 2 prerequisite in `docs/SPRINT-2-IMPLEMENTATION-PLAN.md`. | High |
| TD-008 | Metrics | No historical trend for any metric in `docs/METRICS.md` -- only a single baseline snapshot exists. | Cannot yet detect metric regression over time (e.g. complexity creeping up). | Re-run `scripts/metrics/collect_metrics.py` at the end of every sprint and append to a tracked history file. | Low |
| TD-009 | Frontend | Frontend has no test suite (only `tsc -b` type-checking and a production build were verified). | UI regressions would not be caught automatically. | Add a minimal component test (e.g. Vitest + React Testing Library) once the frontend has more than a health-check shell to test. | Low |
| TD-010 | Domain config | `config/config.example.yaml` documents the intended shape of domain configuration (salary bands, commute radius, matching weights, watchlists, learning priorities) but nothing in the codebase reads it. | None yet -- correct for current scope -- but risk of being mistaken for already wired up. | Implement when the Matching Engine sprint is authorized; until then, leave the flag in `docs/platform/PLATFORM_KERNEL.md` and here as the single source of truth on its status. | Low |
| TD-011 | Security (found via metrics framework) | `pip-audit` run as part of `scripts/metrics/collect_metrics.py` found 22 known vulnerabilities across pinned dependencies: `python-jose 3.3.0` (5, including algorithm-confusion-class issues), `python-multipart 0.0.20` (6), `starlette 0.41.3` (9, pulled in by `fastapi==0.115.6`), `pytest 8.3.4` (1), `ecdsa 0.19.2` (1, transitive via python-jose). | `python-jose` in particular is a known-risky, thinly-maintained JWT library; CareerOS's entire auth framework depends on it. | Evaluate replacing `python-jose` with `PyJWT` (actively maintained, does not share the same CVE history) as a dedicated security task -- not done in this session because a crypto-library swap is a code change with test-coverage implications, not a "safe improvement" to make casually alongside a documentation-heavy sprint. Bump `starlette`/`fastapi`, `python-multipart`, and `pytest` to their latest patched versions in the same pass. | High |
| TD-012 | Architecture (found via Docker Build Review / Sprint 2 Readiness directive) | `orion_kernel` is installed by CareerOS via a repo-relative editable path (`-e ../../../platform/kernel`), which only works because the kernel and its one consumer live in the same repository. A genuinely separate repository (e.g. NDIP) cannot reach across an independent git checkout via a relative path, so the kernel cannot be consumed cross-repository today. See `docs/adr/0003-docker-naming-and-multi-project-isolation.md` Decision 2. | Blocks nothing today (CareerOS is still the only ORION product); would block a second product in its own repository from adopting the kernel until fixed. | When a second product actually needs the kernel: tag a kernel release in this repo and have the new product install it via `pip install git+https://.../orion-kernel.git@<tag>` instead of a relative path (or extract `platform/kernel` to its own repository at that point). Not implemented now, per explicit directive not to build cross-repository sharing yet. | Medium (zero urgency today; blocking the moment a second product is authorized) |
| TD-013 | Architecture (identified in ADR 0004) | `EvidenceLink.subject_id` is a polymorphic reference, not a real foreign key, so cleanup on subject deletion depends on every service remembering to call `delete_evidence_links_for_subject` -- nothing at the database level enforces this for a future entity added to `EvidenceSubjectType`. | Low today (all 4 currently-implemented subject-bearing services correctly call it, verified by test); grows with every future evidence-bearing entity added. | Add a lightweight architectural check (similar to the existing kernel-boundary static check in `scripts/metrics/collect_metrics.py`) that flags any new model implementing a subject role without a corresponding cleanup call, OR accept as permanent, documented service-layer discipline. Not resolved this session -- flagged for a future sprint's judgment call. | Low |
| TD-017 | Dead code (found via Sprint 1.6 review) | `app/repositories/base.py` (32 statements) is not imported anywhere in the application -- confirmed via a full-codebase grep. | None (unused code carries no runtime risk, but adds review/maintenance surface and a permanently-0%-coverage line in every metrics run). | Remove, following this repository's own precedent (TD-R03, TD-R04) for confirmed-unused files -- not done this session pending Chief Architect sign-off on the broader Sprint 1.6 disposition, so as not to make unreviewed code changes ahead of the ARB recommendation. | Low |
| TD-019 | Architecture (Sprint 3 Phase 6 review, Approve-with-Conditions item) | The CV Intelligence -> Career DNA write boundary (`docs/SPRINT-3-ARCHITECTURE.md` Section 4) is stated but not structurally enforced -- nothing prevents a future change from writing to Career DNA tables directly instead of through the existing Sprint 2 service functions. | None yet (no CV Intelligence code exists); becomes a real risk the moment Stage 1 implementation begins. | Condition of Stage 1 Definition of Done: an import-boundary lint rule, or at minimum a named test asserting no direct Career DNA model import from CV Intelligence code. | Medium (blocks Stage 1 sign-off, not blocking Phase 6 approval itself) |
| TD-020 | Testability (Sprint 3 Phase 6 review, Approve-with-Conditions item) | No job-provider test fixture strategy exists yet; provider implementations must not be tested against live external APIs. | None yet (no provider code exists). | Condition of Stage 2 Definition of Done: implement `MockProviderClient` against fixture data before the first real provider client is merged. | Medium (blocks Stage 2 sign-off) |
| TD-021 | Database design (Sprint 3 Phase 6 review, Approve-with-Conditions item) | `MatchResult`/`MatchScoreComponent`'s cascade behavior relative to `Person` was named in the review but not yet implemented (no code exists yet to implement it in). | None yet. | Condition of Stage 3 Definition of Done: implement `ondelete="CASCADE"` from `Person`, consistent with every other Career-DNA-adjacent table. | Low (blocks Stage 3 sign-off) |

## Resolved

| ID | Category | Description | Resolved by |
|---|---|---|---|
| TD-R01 | Bug | UUID/dialect mismatch: token `sub` claim (string) compared directly against a PostgreSQL-typed UUID column, breaking under SQLite. | Explicit `uuid.UUID(...)` parsing in `app/api/deps.py` and `app/api/v1/auth.py`, with 401 on malformed input. Fixed during Sprint 1 verification. |
| TD-R02 | Bug | Readiness check reached for the module-level DB engine directly instead of the request-scoped session, making it untestable without a live Postgres instance. | `/health/ready` (now `orion_kernel.health`) injects the session via `Depends(get_db)`. Fixed during Sprint 1 verification. |
| TD-R03 | Dead code | `app/schemas/health.py` became an unused duplicate once health schemas moved into `orion_kernel.health` during the platform restructure. | Deleted (confirmed no references first) during Sprint 1 Closure repository review (Task 8). |
| TD-R04 | Dead code | Empty `app/utils/` directory left over from initial scaffolding, never used. | Deleted during Sprint 1 Closure repository review (Task 8). |
| TD-R05 | Config hygiene | `products/careeros/backend/.dockerignore` became dead weight after the Docker build context moved to the repository root (ADR 0002) -- a subdirectory `.dockerignore` has no effect on a root-context build. | Removed; replaced by a root-level `.dockerignore`. |
| TD-R06 | Bug (found via live Docker run) | `docker compose up --build`, run by the user on their actual Windows/Docker Desktop machine, crash-looped `careeros_backend` with `pydantic_settings.sources.SettingsError` / `JSONDecodeError` on `CORS_ORIGINS`. Root cause: pydantic-settings treats `List[str]` fields as "complex" and tries to JSON-decode the raw environment variable *before* any pydantic validator runs, so a plain comma-separated value (`.env.example`'s actual format) crashed at the settings-source level, never reaching the existing `_split_cors` before-validator. This session's Docker-less verification (embedded Postgres, direct env var assignment in Python) never set `CORS_ORIGINS` from an env string the way `docker compose --env-file .env` does, so it did not catch this. | Added `NoDecode` annotation (`pydantic_settings.NoDecode`) to `CORS_ORIGINS` in `platform/kernel/orion_kernel/config.py`, which tells pydantic-settings to leave the raw string alone for the existing validator to split. Reproduced the exact crash and confirmed the fix locally (Python, no Docker needed, since the bug is in pydantic-settings' env-parsing, not Docker itself) before reporting fixed; full backend test suite (12/12) re-run and still passing. **Confirmed inside an actual container**: the user re-ran `docker compose up --build` and the backend started cleanly with no crash loop -- see TD-R07. |
| TD-R07 | Verification gap (was TD-007) | `docker compose up --build` had never been executed end-to-end to completion; the equivalent verification (embedded PostgreSQL, live `uvicorn` process, direct HTTP calls) proved the application + database integration but not the container build, inter-container networking, or Docker's own `HEALTHCHECK` directive. | User ran `docker compose up --build` a second time after the TD-R06 fix. Result: backend started cleanly (no crash loop), migrations applied, frontend served on `:5173`, and the log showed recurring successful `GET /api/v1/health` requests at ~30s intervals -- Docker's `HEALTHCHECK` directive itself firing and passing, which no Docker-less verification could have exercised. Full end-to-end Docker verification is now complete; `docs/RiskRegister.md` RT-01 and `docs/SPRINT-1-ACCEPTANCE-REPORT.md` updated accordingly. |
| TD-R08 | Test infrastructure (found via Sprint 1.6 API testing) | SQLite (`tests/conftest.py`'s default fast test backend) does not enforce `PRAGMA foreign_keys`, meaning every `ondelete="CASCADE"` relationship in the schema was silently untested -- a real test (`test_delete_evidence_cascades_and_demotes_dependent_skill`) failed under the old configuration, exposing that deleting Evidence did not actually remove its EvidenceLink rows under SQLite. | Fixed in `tests/conftest.py` via an SQLAlchemy `connect` event listener executing `PRAGMA foreign_keys=ON` for every SQLite test connection. Re-ran the full suite (69/69 passing) to confirm no other cascade-dependent test broke once enforcement was actually turned on. |
| TD-R09 | Bug (found via Sprint 1.6 API testing) | `app/services/employment_service.py`'s conflict-detection for the "one primary current employment per person" constraint checked only for the PostgreSQL-style index-name substring in the raised `IntegrityError`, which SQLite's driver never includes (it reports `"UNIQUE constraint failed: employment.person_id"` instead) -- causing an unhandled 500 instead of the intended 409 under the SQLite test path only. | Fixed via a shared `_is_primary_current_conflict` helper checking both dialects' actual message formats; verified with `test_second_primary_current_employment_rejected` and `test_promote_conflicts_with_existing_primary_current`. |
| TD-R10 | Bug (found via Sprint 1.6 API testing) | `app/main.py`'s `RequestValidationError` handler passed `exc.errors()` directly to `JSONResponse`, crashing with an unhandled `TypeError` whenever a Pydantic `field_validator` raised `ValueError` with contextual args (e.g. `EmploymentCreate.end_after_start`), because the raw exception object embedded in Pydantic's `ctx` dict is not JSON-serializable. | Fixed by encoding `exc.errors()` through `fastapi.encoders.jsonable_encoder` before handing to `JSONResponse`; verified with `test_end_date_before_start_date_rejected`. |
| TD-R11 | Test measurement gap (found via Sprint 1.6 Test Completion) | No `.coveragerc` existed; `coverage.py`'s default tracer does not follow execution across the `greenlet` context switches SQLAlchemy's async engine uses internally, so coverage was silently undercounting all service-layer code. Newly-written tests that verifiably exercised specific branches (confirmed via passing assertions on their side effects) were still reported as "missed." This almost certainly means the original Sprint 1 Closure baseline (83.76%) was also an undercount, not just Career DNA's new code. | Added `products/careeros/backend/.coveragerc` with `concurrency = greenlet`. Re-measured: overall coverage jumped from 83% to 96.01% with the exact same test suite -- no new tests were needed to reveal this, only correct measurement. `docs/metrics.json` regenerated with the corrected figure. |
| TD-R12 | Architecture (found during Sprint 3 governance migration) | The Sprint 3 directive's target repository diagram specified a top-level `/shared` directory, distinct from `/platform/shared_services`, `/shared_libraries`, `/shared_connectors`. This restructure was flagged rather than performed, pending Chief Architect confirmation. | Resolved by Chief Architect Decision 5 (ADR 0005): explicitly deferred, not executed. "The current structure is internally consistent... repository stability is preferable to cosmetic restructuring." Revisit only if a second ORION product requires broader shared libraries -- same trigger condition as TD-012. |
